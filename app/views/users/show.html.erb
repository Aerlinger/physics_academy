<%# Show the notice: %>
<%=  render partial: 'layouts/subheader', locals: {title: "#{@user.name}'s Profile".html_safe} %>

<div class="chalkboard-background">

  <div class="container" style="padding-top: 2em;">

    <div class="row-fluid">
      <%= render partial: "statistics" %>
    </div>

    <div class="row-fluid">
      <%= render partial: "achievements" %>
    </div>

  </div>

</div>